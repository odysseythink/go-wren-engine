{
  "catalog": "wren",
  "schema": "default",
  "models": [
    {
      "name": "customer",
      "refSql": "SELECT * FROM customer",
      "columns": [
        { "name": "custkey", "type": "INTEGER" }
      ]
    }
  ]
}
